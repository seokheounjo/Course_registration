<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>과목 목록</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

<h1>수강 가능 과목 목록</h1>
{{#message}}<p class="message">{{message}}</p>{{/message}}

<table border="1">
    <thead>
    <tr>
        <th>과목명</th>
        <th>교수</th>
        <th>학점</th>
        <th>정원</th>
        <th>신청</th>
    </tr>
    </thead>
    <tbody>
    {{#subjects}}
        <tr>
            <td>{{name}}</td>
            <td>{{professor.name}}</td>
            <td>{{credits}}</td>
            <td>{{capacity}}</td>
            <td>
                {{#enrolledMap.[id]}}
                    <span style="color: green; font-weight: bold;">신청 완료</span>
                {{/enrolledMap.[id]}}
                {{^enrolledMap.[id]}}
                    <form method="post" action="/student/enroll">
                        <input type="hidden" name="subjectId" value="{{id}}">
                        <button type="submit">신청</button>
                    </form>
                {{/enrolledMap.[id]}}
            </td>
        </tr>
    {{/subjects}}
    </tbody>
</table>

</body>
</html>
