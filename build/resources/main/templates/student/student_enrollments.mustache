<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>내 수강 목록</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

<h1>수강 신청 내역</h1>
<table border="1">
    <thead>
    <tr>
        <th>과목명</th>
        <th>교수</th>
        <th>학점</th>
        <th>신청일</th>
        <th>취소</th>
    </tr>
    </thead>
    <tbody>
    {{#subjectInfo}}
        <tr>
            <td>{{@key.name}}</td>
            <td>{{@key.professor.name}}</td>
            <td>{{@key.credits}}</td>
            <td>{{.}}</td>
            <td>
                <form method="post" action="/student/enroll/cancel">
                    <input type="hidden" name="subjectId" value="{{@key.id}}">
                    <button type="submit">신청 취소</button>
                </form>
            </td>
        </tr>
    {{/subjectInfo}}
    </tbody>
</table>

<a href="/student/subjects">← 과목 목록으로 돌아가기</a>

</body>
</html>
