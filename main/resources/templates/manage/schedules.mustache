<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>시간표 관리</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<header class="site-header">…</header>
<main class="page-wrapper">
    <h1>시간표 목록</h1>
    <form method="get" action="/admin/schedules" style="margin-bottom:1rem;">
        <select name="subjectId">
            <option value="">전체 과목</option>
            {{#subjects}}
                <option value="{{id}}" {{#equals id ../selectedSubj}}selected{{/equals}}>{{name}}</option>
            {{/subjects}}
        </select>
        <button type="submit">필터</button>
        <a href="/admin/schedules/add">추가</a>
    </form>
    <table>
        <thead>
        <tr><th>ID</th><th>과목</th><th>요일</th><th>시작</th><th>종료</th><th>강의실</th><th>액션</th></tr>
        </thead>
        <tbody>
        {{#schedules}}
            <tr>
                <td>{{id}}</td><td>{{subjectId}}</td><td>{{dayOfWeek}}</td><td>{{startTime}}</td><td>{{endTime}}</td><td>{{roomId}}</td>
                <td>
                    <a href="/admin/schedules/edit/{{id}}">수정</a> |
                    <a href="/admin/schedules/delete/{{id}}" onclick="return confirm('정말 삭제하시겠습니까?');">삭제</a>
                </td>
            </tr>
        {{/schedules}}
        </tbody>
    </table>
</main>
<footer class="site-footer">…</footer>
</body>
</html>
