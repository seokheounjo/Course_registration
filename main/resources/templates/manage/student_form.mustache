<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>{{#student.id}}학생 수정{{/student.id}}{{^student.id}}학생 추가{{/student.id}}</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<header class="site-header">…</header>
<main class="page-wrapper">
    <h1>{{#student.id}}학생 수정{{/student.id}}{{^student.id}}학생 추가{{/student.id}}</h1>
    <form method="post" action="/admin/students{{#student.id}}/edit/{{student.id}}{{/student.id}}{{^student.id}}/add{{/student.id}}">
        <div class="form-group">
            <label for="studentNumber">학번</label>
            <input id="studentNumber" name="studentNumber" type="text" required value="{{student.studentNumber}}">
        </div>
        <div class="form-group">
            <label for="password">비밀번호</label>
            <input id="password" name="password" type="password" {{^student.id}}required{{/student.id}}>
        </div>
        <div class="form-group">
            <label for="name">이름</label>
            <input id="name" name="name" type="text" required value="{{student.name}}">
        </div>
        <div class="form-group">
            <label for="email">이메일</label>
            <input id="email" name="email" type="email" required value="{{student.email}}">
        </div>
        <div class="form-group">
            <label for="phone">전화번호</label>
            <input id="phone" name="phone" type="text" value="{{student.phone}}">
        </div>
        <div class="form-group">
            <label for="dept">학과</label>
            <select id="dept" name="department.id" required>
                {{#departments}}
                    <option value="{{id}}" {{#equals id ../student.department.id}}selected{{/equals}}>{{name}}</option>
                {{/departments}}
            </select>
        </div>
        <div class="form-group">
            <label for="grade">학년</label>
            <input id="grade" name="grade" type="text" required value="{{student.grade}}">
        </div>
        <button type="submit" class="btn-login">저장</button>
    </form>
</main>
<footer class="site-footer">…</footer>
</body>
</html>
