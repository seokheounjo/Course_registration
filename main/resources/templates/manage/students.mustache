<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>학생 관리</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<header class="site-header">…</header>
<main class="page-wrapper">
    <h1>학생 목록</h1>
    <form method="get" action="/admin/students" style="margin-bottom:1rem;">
        <select name="deptId">
            <option value="">전체 학과</option>
            {{#departments}}
                <option value="{{id}}" {{#equals id ../selectedDept}}selected{{/equals}}>{{name}}</option>
            {{/departments}}
        </select>
        <select name="grade">
            <option value="">전체 학년</option>
            {{#grades}}
                <option value="{{.}}" {{#equals . ../selectedGrade}}selected{{/equals}}>{{.}}</option>
            {{/grades}}
        </select>
        <button type="submit">필터</button>
        <a href="/admin/students/add">추가</a>
    </form>
    <table>
        <thead>
        <tr>
            <th>ID</th><th>학번</th><th>이름</th><th>이메일</th><th>학과</th><th>학년</th><th>액션</th>
        </tr>
        </thead>
        <tbody>
        {{#students}}
            <tr>
                <td>{{id}}</td><td>{{studentNumber}}</td><td>{{name}}</td><td>{{email}}</td>
                <td>{{department.name}}</td><td>{{grade}}</td>
                <td>
                    <a href="/admin/students/edit/{{id}}">수정</a> |
                    <a href="/admin/students/delete/{{id}}" onclick="return confirm('정말 삭제하시겠습니까?');">삭제</a>
                </td>
            </tr>
        {{/students}}
        </tbody>
    </table>
</main>
<footer class="site-footer">…</footer>
</body>
</html>
