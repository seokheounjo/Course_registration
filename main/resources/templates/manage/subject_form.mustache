<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>{{#subject.id}}과목 수정{{/subject.id}}{{^subject.id}}과목 추가{{/subject.id}}</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<header class="site-header">…</header>
<main class="page-wrapper">
    <h1>{{#subject.id}}과목 수정{{/subject.id}}{{^subject.id}}과목 추가{{/subject.id}}</h1>
    <form method="post" action="/admin/subjects{{#subject.id}}/edit/{{subject.id}}{{/subject.id}}{{^subject.id}}/add{{/subject.id}}">
        <div class="form-group">
            <label for="name">이름</label>
            <input id="name" name="name" type="text" required value="{{subject.name}}">
        </div>
        <div class="form-group">
            <label for="credits">학점</label>
            <input id="credits" name="credits" type="number" required value="{{subject.credits}}">
        </div>
        <div class="form-group">
            <label for="capacity">정원</label>
            <input id="capacity" name="capacity" type="number" required value="{{subject.capacity}}">
        </div>
        <div class="form-group">
            <label for="semester">학기</label>
            <input id="semester" name="semester" type="text" required value="{{subject.semester}}">
        </div>
        <div class="form-group">
            <label for="prof">교수</label>
            <select id="prof" name="professorId" required>
                {{#professors}}
                    <option value="{{id}}" {{#equals id ../subject.professorId}}selected{{/equals}}>{{name}}</option>
                {{/professors}}
            </select>
        </div>
        <div class="form-group">
            <label for="dept">학과</label>
            <select id="dept" name="departmentId" required>
                {{#departments}}
                    <option value="{{id}}" {{#equals id ../subject.departmentId}}selected{{/equals}}>{{name}}</option>
                {{/departments}}
            </select>
        </div>
        <button type="submit" class="btn-login">저장</button>
    </form>
</main>
<footer class="site-footer">…</footer>
</body>
</html>
