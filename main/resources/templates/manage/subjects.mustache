<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>과목 관리</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
<header class="site-header">…</header>
<main class="page-wrapper">
    <h1>과목 목록</h1>
    <form method="get" action="/admin/subjects" style="margin-bottom:1rem;">
        <select name="deptId">
            <option value="">전체 학과</option>
            {{#departments}}
                <option value="{{id}}" {{#equals id ../selectedDept}}selected{{/equals}}>{{name}}</option>
            {{/departments}}
        </select>
        <select name="profId">
            <option value="">전체 교수</option>
            {{#professors}}
                <option value="{{id}}" {{#equals id ../selectedProf}}selected{{/equals}}>{{name}}</option>
            {{/professors}}
        </select>
        <input type="text" name="semester" placeholder="학기" value="{{selectedSemester}}">
        <button type="submit">필터</button>
        <a href="/admin/subjects/add">추가</a>
    </form>
    <table>
        <thead>
        <tr>
            <th>ID</th><th>이름</th><th>학점</th><th>정원</th><th>학기</th><th>교수ID</th><th>학과ID</th><th>액션</th>
        </tr>
        </thead>
        <tbody>
        {{#subjects}}
            <tr>
                <td>{{id}}</td><td>{{name}}</td><td>{{credits}}</td><td>{{capacity}}</td>
                <td>{{semester}}</td><td>{{professorId}}</td><td>{{departmentId}}</td>
                <td>
                    <a href="/admin/subjects/edit/{{id}}">수정</a> |
                    <a href="/admin/subjects/delete/{{id}}" onclick="return confirm('정말 삭제하시겠습니까?');">삭제</a>
                </td>
            </tr>
        {{/subjects}}
        </tbody>
    </table>
</main>
<footer class="site-footer">…</footer>
</body>
</html>
